# Copy to .env and fill in. NEVER commit .env.

# AWS Configuration
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=  # Leave blank on EC2 — uses IAM role
AWS_SECRET_ACCESS_KEY=  # Leave blank on EC2 — uses IAM role

# DynamoDB
DYNAMODB_TABLE_ASSETS=riskforge-assets
DYNAMODB_TABLE_FINDINGS=riskforge-findings
DYNAMODB_TABLE_ASSESSMENTS=riskforge-assessments
DYNAMODB_TABLE_RECOMMENDATIONS=riskforge-recommendations

# S3
S3_BUCKET_REPORTS=riskforge-reports-prod
S3_REPORT_URL_EXPIRY=3600

# Bedrock
BEDROCK_MODEL_ID=anthropic.claude-haiku-20240307-v1:0
BEDROCK_MAX_TOKENS=500
BEDROCK_ENABLED=true

# App
APP_ENV=production
APP_PORT=8000
CORS_ORIGINS=*

# Frontend
# Leave blank: Nginx proxies /api on the same origin, so the bundle needs no
# absolute host. Set it only if the API is served from a different origin.
VITE_API_BASE_URL=
